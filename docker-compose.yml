version: '3.8'
services:
  mcp:
    build: .
    image: openai-cli:latest
    container_name: openai-cli
    env_file:
      - .env
    ports:
      - '8080:8080'
    command: ["serve", "--host", "0.0.0.0", "--port", "8080"]